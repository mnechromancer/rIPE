[tool:pytest]
markers =
    performance: marks tests as performance tests (may be slow)
    e2e: marks tests as end-to-end tests (require full setup)
    integration: marks tests as integration tests
    unit: marks tests as unit tests